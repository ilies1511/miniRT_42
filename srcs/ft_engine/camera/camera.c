#include <ft_engine.h>
#include <main.h>

t_ray	sc_ray_for_pixel(t_camera_book camera, double positon_x, double positon_y)
{
	t_point	x_offset;
	t_point	y_offset;

	x_offset = (positon_x + 0.5) * camera.
}
