#include <main.h>
#include <ft_colors.h>
#include <ft_engine.h>
#include <mlx_interface.h>

// can be used to simplify but in the end this has to be removed
// since it will make the program run ALOT slower and removing it should
// be trivial
void	put_pixel(size_t x, size_t y, t_uint_color color)
{
	//placeholder
	(void)x;
	(void)y;
	(void)color;
}
